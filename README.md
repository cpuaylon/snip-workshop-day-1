# bundle (generated branch)

This branch is **generated output**, produced by `scripts/build-bundle.mjs` on
`main`. Do not hand-edit files here — changes will be overwritten the next
time the build script runs. To change what ends up on this branch, edit the
`backend`, `frontend`, or `cli` branches (or the build script itself) instead.
